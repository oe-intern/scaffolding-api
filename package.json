{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "shopify": "shopify",
        "build:app": "shopify app build",
        "dev:app": "shopify app dev",
        "push:app": "shopify app push",
        "generate:app": "shopify app generate",
        "deploy:app": "shopify app deploy",
        "info:app": "shopify app info"
    },
    "devDependencies": {
        "@shopify/app": "^3.58.2",
        "@shopify/cli": "^3.69.0",
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
